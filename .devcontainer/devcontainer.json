{
  "name": "Stegosource",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {},
    "ghcr.io/anthropics/devcontainer-features/claude-code:1": {}
  },
  "forwardPorts": [8501],
  "portsAttributes": {
    "8501": {
      "label": "Stegosource",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "sudo rm -f /etc/apt/sources.list.d/yarn.list && sudo apt-get update || true && sudo apt-get install -y tmux && curl -LsSf https://astral.sh/uv/install.sh | sh && uv pip install --system -e . && npx playwright install --with-deps chrome",
  "customizations": {
    "codespaces": {
      "openFiles": ["README.md"]
    }
  }
}
